# DataSource settings: set here your own configurations for the database
# connection.
spring.datasource.url = jdbc:mysql://localhost/my_play
spring.datasource.username = my_play_dbuser
spring.datasource.password = workhardplayhard

# Keep the connection alive if idle for a long time (needed in production)
spring.datasource.testWhileIdle = true
spring.datasource.validationQuery = SELECT 1

# Show or not log for each sql query
spring.jpa.show-sql = true

# Hibernate ddl auto (create, create-drop, update)
spring.jpa.hibernate.ddl-auto = update

# Naming strategy
spring.jpa.hibernate.naming-strategy = org.hibernate.cfg.ImprovedNamingStrategy

# Use spring.jpa.properties.* for Hibernate native properties (the prefix is
# stripped before adding them to the entity manager)

# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect



# 2nd data source
pure.datasource.url = jdbc:mysql://localhost/my_play
pure.datasource.username = my_play_dbuser
pure.datasource.password = workhardplayhard

server.port=9000

# Comment this line to disable direct default rest api access.
# Keep it only for learning/demo purpose
spring.data.rest.base-path=/api


# for JWT Support
# App configuration
pure.token.header=X-Auth-Token
pure.token.secret=sssshhhh!
pure.token.expiration=604800



